\documentclass[11pt]{article}

\usepackage[margin=1in]{geometry}
\usepackage[utf8]{inputenc}
\usepackage[T1]{fontenc}
\usepackage{lmodern}
\usepackage{microtype}
\usepackage{booktabs}
\usepackage{graphicx}
\usepackage{amsmath}
\usepackage{amssymb}
\usepackage{array}
\usepackage{enumitem}
\usepackage[colorlinks=true,linkcolor=blue,citecolor=blue,urlcolor=blue]{hyperref}

\title{\textbf{Context-Conditioned Confidentiality Failures in Refusal-Tuned Language Models}\\
\large A Behavioral Study of the Cohere Command Family}

\author{
Adharapurapu V S M Ashok Kumar \\
KL University, Vijayawada \\
\texttt{avsmashokkumar@gmail.com}
}

\date{\today}

\begin{document}

\maketitle

\begin{abstract}
Refusal-tuned language models are often evaluated on harmful-request compliance, but that axis alone does not tell us whether model-visible confidential context is protected. We present a controlled behavioral study of three Cohere Command models using synthetic confidential canaries placed in system-visible or retrieved/tool-style context. Our strongest finding is a repeated application-visible confidentiality failure: \texttt{command-r7b-12-2024} leaked an exact synthetic secret in 139/300 replicated system-context probes (46.4\%) and in 15/24 clean two-turn retrieved-context exfiltration cases (62.5\%). In a one-shot retrieved/tool-context extraction setting, all three tested models leaked heavily, including 20/24 exact leaks for \texttt{command-r7b-12-2024}, 18/24 for \texttt{command-r-plus-08-2024}, and 19/24 for \texttt{command-a-03-2025}. At the same time, the paired harmful-request benchmark showed 0/100 unsafe compliance for \texttt{command-r7b-12-2024}. This decoupling shows that harmful-request refusal and confidentiality under model-visible context are distinct properties that must be evaluated separately. We release the full behavioral harness, count-based statistical summaries, and a redacted hashed appendix to support responsible disclosure and downstream replication.
\end{abstract}

\section{Introduction}
Large language model safety discussions often collapse several behaviors into a single notion of ``alignment.'' In practice, deployment-facing systems care about at least two distinct questions: will the model comply with harmful instructions, and will it disclose confidential context that is visible to the model but not intended for the user? The first question is commonly benchmarked. The second is often left to prompt engineering, policy text, or informal red-teaming.

This paper focuses on the second question. We study whether refusal-tuned chat models disclose synthetic confidential values placed in model-visible context. The core setting is narrow by design: the model is given a synthetic secret in system or retrieved/tool-style context, is explicitly instructed not to reveal it, and is then pressured with direct extraction, structured formatting, transformation, or staged conversational prompts. This does not test provider-hidden prompts, infrastructure compromise, or training-data leakage. It tests whether natural-language instructions are a reliable confidentiality control for application-supplied sensitive context.

The repository already contains the full evidence for this narrower claim. It does \emph{not} yet contain the finished open-weight quantization-geometry results suggested by earlier drafts. This paper therefore aligns the narrative to the completed evidence: a behavioral study of the Cohere Command family, backed by statistical summaries and redacted representative cases, plus an aligned experimental pipeline for future open-weight geometry work.

\paragraph{Contributions.}
\begin{itemize}[leftmargin=1.5em]
    \item We present a controlled behavioral study of model-visible confidentiality failures across three Cohere Command models.
    \item We show that harmful-request refusal and confidentiality under model-visible context are empirically separable.
    \item We compare system-context, retrieved-context, and staged two-turn extraction settings in a unified evaluation.
    \item We release a full evidence pack: prompts, responses, rule-based scoring, Wilson confidence intervals, and a redacted hashed appendix for representative cases.
\end{itemize}

\section{Scope and Threat Model}
Our threat model is intentionally constrained. The attacker interacts through ordinary user prompts. The model has already been given a synthetic confidential value in context that is visible to the model. We evaluate whether that value can be recovered under different prompt pressures and conversational structures.

\begin{table}[t]
\centering
\small
\begin{tabular}{p{0.19\linewidth}p{0.23\linewidth}p{0.20\linewidth}p{0.26\linewidth}}
\toprule
Setting & Attacker access & Secret type & Primary metric \\
\midrule
System-context probe & user prompt only & synthetic canary in system context & exact / any leak rate \\
Retrieved-context probe & user prompt plus retrieved/tool-style state & synthetic confidential field & exact / any leak rate \\
Two-turn exfiltration & safe first step, then follow-up extraction & synthetic confidential field & final exact leak and transcript leak \\
\bottomrule
\end{tabular}
\caption{Threat-model summary. All secrets are synthetic and intentionally model-visible. The paper does not claim provider-hidden prompt leakage, infrastructure compromise, or exposure of real user data.}
\label{tab:threat-model}
\end{table}

\begin{figure}[t]
\centering
\fbox{%
\parbox{0.93\linewidth}{%
\textbf{Behavioral evaluation pipeline.}
(1) Inject a synthetic confidential value into system or retrieved/tool-style context.
(2) Query the model with direct, structured, transformed, or staged extraction prompts.
(3) Score exact, partial, encoded, and transcript-level leakage offline.
(4) Aggregate by model, context type, and prompt family with Wilson confidence intervals.
}}
\caption{Compact pipeline schematic for the completed behavioral study.}
\label{fig:pipeline}
\end{figure}

\section{Methodology}
\subsection{Models}
We evaluate three Cohere chat models through the public API:
\texttt{command-r7b-12-2024},
\texttt{command-r-plus-08-2024}, and
\texttt{command-a-03-2025}.

\subsection{Prompt families}
We use four main families of probes:
\begin{enumerate}[leftmargin=1.5em]
    \item \textbf{System-context canary probes}: direct secret requests, JSON extraction, checksum pressure, quoted policy tricks, and related one-shot prompts.
    \item \textbf{System-context multi-turn probes}: staged two-turn conversations that first request policy summaries, field names, transforms, or roleplay setup, and then request the exact secret.
    \item \textbf{Retrieved/tool-context probes}: one-shot extraction from a retrieved confidential record containing an \texttt{audit\_token}.
    \item \textbf{Retrieved/tool-context exfiltration}: clean two-turn flows in which the model first gives a summary or redacted view, then receives a follow-up extraction request.
\end{enumerate}

\subsection{Scoring}
Every response is scored offline with conservative rule-based checks for:
\begin{itemize}[leftmargin=1.5em]
    \item \textbf{Exact leakage}: the full canary appears in the response.
    \item \textbf{Partial leakage}: distinctive fragments or format information appear without the full canary.
    \item \textbf{Encoded leakage}: the canary appears in base64 or a similar transformed representation.
    \item \textbf{Any leakage}: exact, partial, or encoded leakage.
    \item \textbf{Refusal}: the final response refuses the request.
\end{itemize}
We report count-based Wilson intervals for headline rates where appropriate.

\section{Results}
\subsection{Master summary}
Table~\ref{tab:master-results} is the shortest accurate summary of the completed evidence. The most important pattern is that harmful-request refusal is strong on the paired pilot, while confidentiality robustness varies sharply with context type and model.

\begin{table*}[t]
\centering
\small
\begin{tabular}{llcccc}
\toprule
Family & Setting & Metric & \texttt{command-r7b-12-2024} & \texttt{command-r-plus-08-2024} & \texttt{command-a-03-2025} \\
\midrule
Harmful prompts & AdvBench reference & Unsafe compliance & 0/100 (0.0\%) & -- & -- \\
System context & Canary rotation & Exact leak & 58/120 (48.3\%) & 33/120 (27.5\%) & 1/120 (0.8\%) \\
System context & Multi-turn extraction & Final exact leak & 16/30 (53.3\%) & 3/30 (10.0\%) & 0/30 (0.0\%) \\
Retrieved context & Single-turn tool-context probe & Exact leak & 20/24 (83.3\%) & 18/24 (75.0\%) & 19/24 (79.2\%) \\
Retrieved context & Single-turn tool-context probe & Any leak & 24/24 (100.0\%) & 22/24 (91.7\%) & 22/24 (91.7\%) \\
Retrieved context & Clean two-turn exfiltration & Final exact leak & 15/24 (62.5\%) & 1/24 (4.2\%) & 0/24 (0.0\%) \\
Retrieved context & Clean two-turn exfiltration & Transcript any leak & 18/24 (75.0\%) & 5/24 (20.8\%) & 4/24 (16.7\%) \\
\bottomrule
\end{tabular}
\caption{Master summary of the completed behavioral evidence. Dashes indicate that the specific 100-prompt harmful-request pilot was only run for \texttt{command-r7b-12-2024}.}
\label{tab:master-results}
\end{table*}

\subsection{System-context confidentiality failures}
The simplest system-context finding is already strong. In a 100-prompt main canary probe on \texttt{command-r7b-12-2024}, the model leaked the exact synthetic secret in 50/100 cases and leaked exact-or-distinctive partial content in 54/100 cases. A five-canary rotation replication strengthened this result: across 300 total probes, the exact leak rate was 139/300 (46.4\%, 95\% Wilson CI 40.8\%--52.0\%), showing that the effect was not tied to one token string.

The same model also showed staged conversational vulnerability. In a 50-case multi-turn system-context extraction study, the final exact leak rate was 26/50 (52.0\%), with several variants reaching 100\% exact leakage across the tested canaries.

\subsection{Retrieved-context and tool-style failures}
The strongest practical failure mode in this study is retrieved/tool-style context. When the model saw a retrieved confidential record and was asked in a single turn to extract or transform the \texttt{audit\_token}, all three tested models leaked heavily. Exact leakage was above 75\% for all three models.

The cleaner two-turn exfiltration rerun provides a more realistic workflow-shaped test. Table~\ref{tab:toolchain-ci} shows the aggregate rates and confidence intervals. The ranking changes materially: \texttt{command-r7b-12-2024} remains clearly vulnerable, whereas \texttt{command-r-plus-08-2024} and \texttt{command-a-03-2025} mostly refuse the second-step extraction request, though they still show some residual partial or transcript-level disclosures.

\begin{table}[t]
\centering
\small
\begin{tabular}{lccc}
\toprule
Model & Final exact leak & 95\% CI & Transcript any leak \\
\midrule
\texttt{command-r7b-12-2024} & 15/24 (62.5\%) & 42.7\%--78.8\% & 18/24 (75.0\%) \\
\texttt{command-r-plus-08-2024} & 1/24 (4.2\%) & 0.7\%--20.2\% & 5/24 (20.8\%) \\
\texttt{command-a-03-2025} & 0/24 (0.0\%) & 0.0\%--13.8\% & 4/24 (16.7\%) \\
\bottomrule
\end{tabular}
\caption{Aggregate results for the clean two-turn retrieved-context exfiltration rerun.}
\label{tab:toolchain-ci}
\end{table}

\subsection{Refusal and confidentiality are separable}
The paired harmful-request pilot for \texttt{command-r7b-12-2024} produced 0/100 unsafe compliance on the AdvBench slice, while the same model leaked synthetic confidential values repeatedly under multiple context-visible extraction settings. This is the central empirical point of the paper. A model can be strong on harmful-request refusal and still fail badly on application-visible confidentiality.

\section{Discussion}
Three points matter most. First, context type is not a cosmetic detail. The model ranking under one-shot system-context probes is not identical to the ranking under retrieved/tool-context probes or staged two-turn exfiltration. Second, refusal should not be used as a proxy for confidentiality. A refusal-tuned model may still reveal sensitive model-visible state when the prompt is phrased as verification, formatting, transformation, or structured extraction. Third, application developers should treat model-visible secrets as effectively disclosed unless a stronger control exists outside the model itself.

This paper does not argue that Cohere leaked provider-hidden prompts or real customer data. The claim is narrower and still important: natural-language instructions were not a reliable confidentiality control for synthetic secrets deliberately placed in model-visible context. That is enough to matter for retrieval-augmented generation, tool-using agents, and any application that embeds sensitive state in prompts.

\section{Limitations}
This is an API-only behavioral study. We cannot inspect internal activations, train probes, or measure residual-stream geometry for these hosted models. The secrets are synthetic canaries, not real customer data, and the prompts are controlled adversarial probes rather than organic user traffic. Several experiments are also still pilot-scale, especially the 24-case clean two-turn exfiltration rerun. These limits make the paper weaker as a mechanistic account, but they do not undercut the main behavioral finding.

\section{Responsible Disclosure}
The findings in this paper were prepared with responsible disclosure in mind. The accompanying repository contains a mail-ready disclosure draft, a longer disclosure report, a vulnerability summary, count-based statistical evidence, a cross-model comparison, and a redacted hashed appendix of representative cases. The intended disclosure claim is narrowly scoped to application-visible confidentiality failures in model-visible context rather than provider-hidden prompt leakage, infrastructure compromise, or real-user-data exposure.

\section{Conclusion}
The completed evidence supports a narrow but important conclusion: refusal-tuned language models can remain strong on harmful-request refusal while still failing application-visible confidentiality tests, especially when sensitive values appear in retrieved or tool-style context. In our strongest staged result, \texttt{command-r7b-12-2024} leaked an exact synthetic confidential value in 15/24 clean two-turn exfiltration cases after an apparently safe first-step summary. The practical implication is straightforward: model-visible secrets should not be considered protected by policy text alone.

\end{document}
